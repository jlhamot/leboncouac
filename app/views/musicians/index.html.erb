<h3>Liste des musiciens</h3>

<p>Liste des musiciens cherchant à jouer dans un groupe. Contactez les !</p>

<table>
  <tr>
    <th>Nom</th>
    <th>Instruments</th>
    <th>Département</th>
    <th>Ville</th>
  </tr>

<% @musicians.each do |musician| %>
  <tr>
    <td><%= musician.name %></td>
 	<td><%= musician.instruments.map { |instrument| instrument.name }.join(", ") %></td>
    <td><%= musician.department %></td>
    <td><%= musician.city %></td>

    <!--<td><%= link_to 'Show', musician %></td>
    <td><%= link_to 'Edit', edit_musician_path(musician) %></td>
    <td><%= link_to 'Destroy', musician, confirm: 'Are you sure?', method: :delete %></td>-->
  </tr>
<% end %>
</table>

<br/>
